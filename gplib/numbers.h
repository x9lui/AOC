#ifndef GPLIB_NUMBERS
#define GPLIB_NUMBERS

/*
*   Calculate the number of digits of a number NUM with base BASE
*
*   @param num is the number whose digits will be calculated
*   @param base the base of the number NUM
*
*   @return The number of digits of NUM
*/
unsigned long_number_of_digits(long num, unsigned base); 

#endif